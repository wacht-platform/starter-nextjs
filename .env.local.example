# Wacht keys — get them from https://console.wacht.dev
# Public key is shipped to the browser.
NEXT_PUBLIC_WACHT_PUBLISHABLE_KEY=pk_test_xxx

# Server-side machine API key (optional for the starter, required for management calls).
WACHT_API_KEY=wk_live_xxx
